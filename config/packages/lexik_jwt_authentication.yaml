lexik_jwt_authentication:
    secret_key: '%env(resolve:default:JWT_SECRET_KEY:JWT_SECRET_KEY)%'
    public_key: '%env(resolve:default:JWT_PUBLIC_KEY:JWT_PUBLIC_KEY)%'
    pass_phrase: '%env(default:JWT_PASSPHRASE:JWT_PASSPHRASE)%'
