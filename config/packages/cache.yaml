framework:
    cache:
        app: cache.adapter.redis
        default_redis_provider: '%env(default::REDIS_CACHE_URL)%'
        pools:
            cache.rate_limiter:
                adapter: cache.adapter.redis
                provider: '%env(default::REDIS_RATE_LIMITER_URL)%'
